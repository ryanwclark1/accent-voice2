---
description: React 19 + Tailwind CSS + ShadCN + TypeScript Style Guide
globs: *.ts, *.tsx, *.css, *.html, *.json
---

# TypeScript + React 19 + TanStack + Tailwind CSS + ShadCN

## **1. Project Structure**
 - Using Tanstack

## **2. TypeScript Configuration**
- **Strict Mode:** Enabled (`strict: true` in `tsconfig.json`)
- **Type Safety:** Use explicit types for props, avoid `any`.
- **Interfaces vs. Types:** Use interfaces for objects, types for 

## **3. Tailwind CSS & Styling**
- **Version:** Tailwind CSS v4

## **4. ShadCN Structure**
- "components": "@/components",
- "utils": "@/lib/utils",
- "ui": "@/components/ui",
- "lib": "@/lib",
- "hooks": "@/hooks"

## **7. Routing & Navigation**
- **Use TanStack Router**
- **Define routes in routes folder**




